# Flujo de desarrollo

Constitución → especificación → clarificación → plan → tareas → implementación incremental → tests → validación.

Cada funcionalidad debe vivir en `specs/<id>-<slug>/` con `spec.md`, `plan.md`, `tasks.md` y `validation.md`. Los cambios de alcance actualizan primero la especificación.
